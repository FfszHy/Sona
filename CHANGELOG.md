# 变更记录

本文件整理已有开发记录，版本号表示源码中的功能演进，不代表已经发布 GitHub Release。

## 0.6.0

首个提供安装包的预览版（`make release` 生成 `build/release/Sona-0.6.0.pkg`；未签名 Developer ID、未公证，首次打开需在「隐私与安全性」中允许）。

- 将音频引擎移至独立 Sona Audio Service；HAL 插件只负责传输与虚拟设备。
- 增加共享内存协议、对端签名校验、会话替换与安全退休机制。
- 修复不同 IO 块大小应用同时播放时的混音时间线问题。
- 支持新设备接入自动切换默认输出。
- 整理源码预览版文档、兼容性记录与贡献指南；`test-all` 纳入 App 测试。
- 新增 pkg 安装器（驱动、服务、App 一次安装，安装后自动启动服务并重载 coreaudiod）和卸载脚本。

## 0.5.1

- 异步写入设备音量，合并拖动期间的更新，改善非默认设备滑块响应。

## 0.5.0

- 多设备独立音量与静音，优先使用硬件控制，必要时数字回退。

## 0.4.0

- 默认输出时钟跟随，同采样率且锁定时使用直通路径。

## 0.3.0

- 加窗 sinc 重采样、平滑时钟漂移补偿和断流恢复。

实现细节和原始回归步骤见 [历史开发记录](docs/DEVELOPMENT-NOTES.md)。
