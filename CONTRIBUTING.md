# 贡献指南

先阅读 [README](README.md) 和 [架构规范](ARCHITECTURE.md)。当前为源码预览版，贡献应包含问题场景、改动理由和实际验证范围。

## 本地检查

```sh
make test-all
make
```

涉及 IPC、对端身份或服务生命周期时，再执行 `make test-e2e`。该测试会在当前用户的 launchd 域临时注册测试服务，可能打开真实音频输出，结束后卸载；不要与其他 Sona 用户域服务测试同时运行。它不等价于系统域宿主验收。

涉及设备路由或音频行为时，按 [兼容性记录](docs/COMPATIBILITY.md) 补充实机结果。请勿只根据离线测试宣称音质、延迟或所有硬件兼容性。

## 修改约束

- 驱动不得直接或间接调用 Core Audio client HAL API。
- 实时音频路径避免阻塞、内存分配、文件 IO 和同步 RPC。
- 保持共享内存格式、生命周期与版本校验一致。
- 为行为修复补充能够复现问题的测试。
- 不提交构建产物、本机配置、凭据或含个人信息的日志。

## 报告问题

请提供源码版本、macOS 版本与构建号、芯片架构、输出设备及连接方式、采样率、复现步骤、预期和实际结果，以及是否能在系统输出改回物理设备后恢复。必要时附已脱敏的服务状态和日志。
