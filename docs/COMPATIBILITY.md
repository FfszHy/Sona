# 兼容性与验证记录

## 支持边界

| 项目 | 当前声明 | 验证范围 |
| --- | --- | --- |
| 菜单栏 App | macOS 26+，arm64 | Package.swift、Info.plist 和 Makefile 一致；不等于所有版本实机通过 |
| 驱动 / 服务 | macOS 14 编译目标，arm64 + x86_64 | 不代表完整产品支持 macOS 14 或 Intel |
| 本次检查环境 | macOS 27.0（26A428），arm64 | 2026-09-20 本地环境 |
| macOS 26 | 声明最低版本 | 尚无该版本完整实机验证记录 |
| Intel Mac | App 未提供对应构建 | 未验证 |

## 本次自动检查（2026-09-20）

| 检查 | 结果 |
| --- | --- |
| `make test-all` | 通过：引擎、音频/SRC、App 音量写入、传输、时钟、边界检查工具与最终驱动导入检查 |
| `make` | 通过：驱动、服务和 App 构建目标完成，App 已本地签名 |
| Git 忽略规则 | 已确认排除构建目录、缓存、`.DS_Store` 和 `.claude/` |
| `make test-e2e` | 本次未运行；此次未修改 IPC 或服务生命周期 |

本次不安装系统驱动、不重启 coreaudiod。首次受沙箱缓存写入限制的构建失败后，已在允许写入编译缓存的环境重跑并通过。

## 既有实机记录

以下来自 [架构记录](../ARCHITECTURE.md)，不是本次重新运行的结果：

| 日期与环境 | 场景 | 记录及边界 |
| --- | --- | --- |
| 2026-09-19，macOS 27.0 26A428，Apple Silicon | 网易云音乐 → 扬声器 | 正常出声；状态报告时钟锁定、直通、underruns 0，无丢块 |
| 同上 | 扬声器 + BlackHole 多输出 | 两个 sink active；BlackHole 使用 SRC，数字音量和静音状态正确回推；不证明两个物理设备听感或同步 |
| 同上 | 逐应用增益 | 配置与状态回推通过；用户听到的变化未单独确认 |
| 2026-09-20，见架构记录 | 128/512 帧双客户端回环 | 混音缺陷修复后的测试记录：late 0、resets 1、丢块 0；不代表长时间实机播放 |

历史记录未完整注明硬件型号、连接方式和测量时长，因此不扩展为普遍兼容承诺。

## 发布前实机验证表

以下均待完整验证。每次记录日期、源码版本、macOS 构建号、Mac 型号、输出设备、连接方式、采样率、运行时长、预期与实际结果。

| 场景 | 操作与通过条件 | 状态 |
| --- | --- | --- |
| 安装 / 升级 / 卸载 | 从干净环境安装；升级后恢复播放；卸载后系统输出可正常使用 | 待测 |
| 逐应用控制 | 两个应用播放；独立调节音量/静音，另一应用不受影响 | 待测 |
| 双物理输出 | 路由至两个物理设备，分别调音量/静音并检查外部硬件变化同步 | 待测 |
| 睡眠唤醒 | 播放中和空闲时睡眠再唤醒，记录恢复时间、丢帧及爆音 | 待测 |
| 蓝牙 / USB 热插拔 | 播放中接入、拔出与重连，检查自动切换及回退 | 待测 |
| 服务崩溃恢复 | 在专用测试环境终止服务，检查 launchd 拉起、声音恢复和旧区域回收 | 待测 |
| coreaudiod 重启 | 在专用测试环境重启宿主，检查重连及时间线恢复 | 待测 |
| 采样率切换 | 切换物理输出采样率，检查恢复、直通/SRC 状态与异常计数 | 待测 |
| 多小时播放 | 持续播放并记录 underrun、drop、漂移、CPU 和能耗 | 待测 |
| 端到端延迟 | 使用硬件回环，记录方法、样本数及延迟分布 | 待测 |
| 快速用户切换 | 两个登录用户间切换，检查服务与路由归属 | 待测 |

离线合成信号测试验证算法和部分生命周期，不证明真实设备延迟、能耗或音质。系统域崩溃与重启测试会中断音频，应在专用测试时段执行。
